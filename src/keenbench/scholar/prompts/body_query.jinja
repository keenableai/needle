You generate a search query for a scholarly-search benchmark. The query must be answerable ONLY by an engine that has indexed the full text of a specific paper — not just its title and abstract.

You are given a paper's title, its abstract, and an excerpt from the body (methods, results, figures). Produce ONE keyword-style search query that targets a specific, distinctive detail found in the BODY but NOT stated in the title or abstract.

The query must be anchored on a GLOBALLY DISTINCTIVE term that THIS paper introduces, names, builds, or measures — one that, on its own, already narrows the web to a handful of papers:
- a method, model, tool, software package, dataset, or benchmark the paper introduces or uses by name (e.g. "Toom's rule", "GC-MS carvacrol", "REDCap")
- a named study site, cohort, organism, cell line, reagent, or instrument
- a precise measured value that is itself unusual (e.g. "carvacrol 80.43%"), NOT a round number or a vague range

Ask yourself: would this query, typed into a scholarly search engine, plausibly return THIS paper near the top? If the query is built only from generic field vocabulary (e.g. "network depth", "layer contribution", "accuracy improvement", "training loss"), it will match thousands of papers — reject it and either find a distinctive anchor or respond NO_DISTINCT_QUERY.

Shared configuration values are NOT distinctive: token counts, context-window sizes, batch sizes, parameter counts, learning rates, epochs, and similar hyperparameters recur across thousands of papers (e.g. "128 tokens", "128K context", "7B parameters", "batch size 32", "512 hidden dim"). Never anchor on these. Anchor on the specific NAME the paper gives its own artifact, or on a genuinely unusual measured result.

Requirements:
- Keyword-style, 3-8 words. Not a natural-language question. Do not start with what/why/how.
- At least one word must be a globally distinctive proper noun, named entity, or unusual precise value — not generic jargon and not a shared hyperparameter/config value.
- Do NOT lead with, or build the query around, a percentage range, a round number, or a common configuration value.
- The query must NOT be answerable from the title or abstract alone: at least two of its content words must NOT appear in the title or abstract.
- Anchor on what THIS paper contributes — NOT on prior work it cites and NOT on a well-known shared result. Specifically:
  - Do NOT use a citation or reference: no "et al", no author-surname strings (e.g. "Smith et al 2024" or "Johnstone Güdel"). Cited names point at OTHER papers.
  - Do NOT anchor on an eponymous or textbook formula, theorem, law, transform, or distribution that predates and is shared across the field (e.g. "Cardano's formula", "Navier-Stokes", "Kermack-McKendrick", "Fourier transform").
- Do NOT reference the paper's own figures, tables, sections, theorems, or supplementary material by number (e.g. "Table 3", "Figure 2", "Theorem 7.1", "Supplementary Table") — those are internal pointers, not searchable content.
- Do NOT copy the title. Do NOT include the paper's arXiv id, DOI, or a URL.
- Always respond in English.
- If the body has no detail distinctive enough to single this paper out, respond with exactly NO_DISTINCT_QUERY.

Title: {{ title }}
Abstract: {{ abstract }}
Body excerpt:
{{ body }}

Respond with ONLY the query string (or NO_DISTINCT_QUERY). No quotes, no explanation, no JSON.
